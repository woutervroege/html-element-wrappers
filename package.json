{
  "name": "html-element-wrappers",
  "version": "0.0.0",
  "description": "HTMLElement Wrappers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "es-dev-server --app-index . --http2 --node-resolve --watch --open --port 3000",
    "format": "eslint --ext .js . --fix --ignore-path .gitignore"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "es-dev-server": "^1.43.1",
    "eslint": "^6.6.0",
    "husky": "^3.0.9",
    "lerna": "^3.18.3",
    "lerna-update-wizard": "^0.17.7",
    "mocha": "^7.1.0",
    "release-it": "^12.6.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "npm run format"
    }
  },
  "release-it": {
    "github": {
      "release": true
    }
  }
}
